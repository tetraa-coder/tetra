#include "modifier_vente.h"
#include "ui_modifier_vente.h"

modifier_vente::modifier_vente(QWidget *parent) :
    QDialog(parent),
    ui(new Ui::modifier_vente)
{
    ui->setupUi(this);

}

modifier_vente::~modifier_vente()
{
    delete ui;
}
